import React from 'react';
import Header from '../Header/Header';

const About = () => {
    return (
        <div>
            <Header></Header>
            <h2>This is the true about page</h2>
        </div>
    );
};

export default About;